from openalea.lpy import *

module Apex()
module Metamer(metamer_data)

production:

interpretation:
maximum depth: 10

Metamer(metamer_data):
    nproduce SetColor(1 if metamer_data.index % 2 else 2)
    nproduce F(0.1, 0.1 if metamer_data.index % 2 else 0.2)
    
Apex():
    produce SetColor(3)@O(0.15)

endlsystem